#include <stdio.h>

#include <postgresql/libpq-fe.h>

#include "../bibliotecas/lecturaRS.h"

#include "../bibliotecas/lecturaPSQL.h"

int main(){
    //Eval *a = leerDB(1);

    int **b = (int **) malloc(sizeof(40));
    //freeM((void**) b, 1);
    printf("%d\n", foo(2,3));
    return 0;
}